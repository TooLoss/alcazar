# Alcazar

## Build project

This project uses CMake (version 3.10 or higher) to manage builds. Follow the
instructions below to compile the game on your local machine.

1. Clone the repository :

```bash
git clone https://github.com/TooLoss/alcazar.git
cd alcazar
```

2. Generate build files:

```bash
mkdir build
cmake --build build
```
